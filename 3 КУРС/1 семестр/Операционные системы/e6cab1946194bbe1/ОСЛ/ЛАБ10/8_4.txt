#include <stdio.h>
#include <unistd.h>
#include <errno.h>
#include <sched.h>
#include <sys/time.h>
#include <sys/resource.h>

struct rlimit rlim;
int main()
{
int ret;
ret = getrlimit (RLIMIT_RTTIME, &rlim);
if (ret == -1) { 
perror ("getrlimit"); 
return 1;
}
printf ("Ћимиты RLIMIT_RTTIME: м€гкий=%ld жесткий=%ld\n", 
rlim.rlim_cur, rlim.rlim_max);

return 0;
}